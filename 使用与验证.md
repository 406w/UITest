# ERP UI 自动化：运行与扩展

工程已按 readme 中确认的设计实现，实际代码使用当前 `../public/app.js` 的 DOM。测试框架不需要修改 ERP 前后端。

## 已实现的场景

| 用例类 | 验证内容 | 环境恢复 |
| --- | --- | --- |
| TestLogin | 有效账号登录、首页用户名称、刷新后会话保留 | 通过页面退出登录 |
| TestInvalidPassword | 错误密码提示、不生成会话 Cookie | 若出现意外登录则退出 |
| TestLogout | 点击退出、清除会话 Cookie、刷新仍显示登录页 | 兜底退出 |
| TestAddSaleOrder | 两行商品销售草稿、金额、字段、编号、刷新后持久化 | 取消本次创建的草稿并退出 |
| TestAddPurchaseOrder | 采购草稿、金额、字段、编号、刷新后持久化 | 取消本次创建的草稿并退出 |

订单使用 `uitest-<UUID>` 备注定位，只取消备注完全匹配的草稿。ERP 不提供订单删除接口，因此会保留已取消的测试订单和审计日志，不恢复数据库序列号；不会审批、出入库、结算或重置演示数据。默认数据依赖演示账号、商品 P001/P002、上海主仓及 YAML 中的往来单位。

## 安装

需要 Python 3.11+、Edge/Chrome/Firefox 中的一种浏览器。独立验证模式还需要 Node.js 24+。Windows 默认使用无头 Edge。

```powershell
cd G:\Project\ERPsystem\ERP-UITest
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.lock.txt
```

`requirements.lock.txt` 是本机验证版本；`requirements.txt` 是允许的主版本范围。已创建的 `.venv` 可直接使用。Python 不在 PATH 时将首行中的 python 替换为本机解释器绝对路径。

## 一键隔离验证

```powershell
.\.venv\Scripts\python.exe main.py --isolated -q
```

程序在空闲端口启动真实 ERP，使用 `.runtime/<run_id>/erp-data` 中的独立 SQLite 数据库，并仅为这个新实例注入项目演示密码。每条用例独立创建浏览器；运行结束停止本次 Node 服务，保留数据库与服务日志用于复查。不启动或修改现有 8090 服务。

首次运行 Selenium Manager 可能下载对应浏览器驱动，缓存位于 `.runtime/selenium`。离线环境可指定 `--driver-path`，浏览器安装位置可用 `--browser-binary` 指定。不要关闭浏览器安全沙箱；若受限执行环境阻止浏览器子进程，请在正常权限的本地终端运行。

## 对已有 ERP 服务运行

先启动 ERP 服务，再设置对应环境的测试账号密码；密码不存入 YAML 或测试 SQLite：

```powershell
$env:ERP_TEST_PASSWORD = '<测试环境密码>'
.\.venv\Scripts\python.exe main.py --base-url http://localhost:8090 -q
```

默认账号为 admin；如需修改账号、商品或仓库，编辑 `data/data.yaml`，或用 `--data-file` 指向另一份数据。对已有环境运行会创建并取消两张测试订单，日志会保留。

常用参数：

```powershell
# 单独运行登录场景
.\.venv\Scripts\python.exe main.py --isolated testcase/test_login.py -q
# 仅冒烟用例（4 条，不含错误密码）
.\.venv\Scripts\python.exe main.py --isolated -m smoke -q
# 显示浏览器 / 使用 Chrome / 调整等待时间
.\.venv\Scripts\python.exe main.py --isolated --headed --browser chrome --ui-timeout 15
# 可选的每次点击前后截图；默认在步骤开始和失败时截图
.\.venv\Scripts\python.exe main.py --isolated --click-snapshots
```

也可直接使用 `.venv\Scripts\python.exe -m pytest`；此时需自行配置外部 ERP 和密码，不会自动启动隔离实例。直接 pytest 和 IDE 运行也会使用默认报告配置，无需另加 --alluredir。

测试基础数据库统一生成在工程绝对路径 `.runtime/ui-data/<会话UUID>/db.db`，通过 main.py、直接 pytest 或 IDE 运行均不依赖系统的 `pytest-of-用户名` 临时目录。每次会话使用独立副本，结束时关闭连接并保留数据供排查；不需要删除或修改系统临时目录的权限。

## 报告

报告配置统一位于 `pytest.ini`，适用于 main.py、直接 pytest 和 IDE：

```ini
required_plugins = allure-pytest
log_level = INFO
erp_report_dir = report
erp_report_html = true
erp_allure_cli = G:/Python/allure/allure-2.45.0/bin/allure.bat
```

每次运行默认保存 `report/allure-results/<run_id>`，并自动生成 `report/allure-report/<run_id>/index.html`。目录相对工程根目录解析，不受 IDE 工作目录影响，默认不会混入旧结果。控制台会显示路径和打开命令，`report/latest.json` 记录本次结果、HTML 路径和报告生成错误。

Allure HTML 生成需要 Allure CLI 及其所需 Java 运行环境。本机路径已配置；迁移机器时可修改配置，也可通过 `--allure-cli` 或 `ALLURE_CLI` 环境变量覆盖（优先级为命令行、环境变量、ini；都留空时查 PATH）。缺少 CLI 或生成失败时，保留原始结果并在终端明确提示，不覆盖 pytest 的测试退出状态。测试失败同样生成报告；--collect-only 不生成。

```powershell
# 默认生成原始结果和 HTML，--html 仍可兼容使用
.\.venv\Scripts\python.exe main.py --isolated -q
# IDE 等价的直接运行方式（需要现有 ERP 服务）
.\.venv\Scripts\python.exe -m pytest testcase/test_login.py -q
# 只要原始结果，不自动生成 HTML
.\.venv\Scripts\python.exe -m pytest --no-report-html
# 或对已有结果单独生成与打开
allure generate report/allure-results/<run_id> -o report/allure-report/<run_id>
allure open report/allure-report/<run_id>
```

本机无需额外设置 PATH 即可生成报告。打开报告可复制控制台打印的完整 PowerShell 命令；若使用上方简写的 allure 命令，则需将其目录加入 PATH。不要直接双击普通 HTML 报告的 index.html；使用 `allure open` 提供本地访问服务。显式 --alluredir 仍按 pytest 的原语义相对当前工作目录解析，不自动清空已有内容。

标题步骤覆盖从声明到下一个标题之间的代码，最后一步在生命周期结束时自动关闭。每次 step/checkpoint 开始都会自动截取当前浏览器画面，以“步骤开始：标题”为附件名加入该步骤；在最终 Allure 报告展开步骤即可查看。截图发生在后续业务动作执行之前，因此首次打开网页的步骤可能记录浏览器初始空白页。AW 自动补充默认标题时同样截图，沿用已有标题不会重复截图。

检查点失败保留断言信息，并额外附加失败时的截图；开始截图不影响失败证据采集。密码输入日志显示 `***`，截图隐藏密码框、标记为敏感的输入框和演示密码提示；不采集完整 HTML 或 Cookie。`--no-screenshots` 可关闭所有截图。无浏览器或截图失败时，不中断业务步骤；截图异常记录到日志。

## 编写用例

```python
from common.test_case import TestCase
from common.report import step, checkpoint

class TestExample(TestCase):
    __test__ = True

    def init(self):
        self.title = "示例：登录并校验用户"
        self.account = self.database.account("admin")

    def setup(self):
        pass

    def process(self):
        step("步骤1：打开网页")
        login = self.aw.open_webpage()

        step("步骤2：登录")
        home = (login.enter_username(self.account.username)
                .enter_password(self.account.password).submit_success())

        checkpoint("检查点1：当前用户正确")
        self.checks.equal(home.current_user(), self.account.expected_display_name, "当前用户")

    def teardown(self):
        self.aw.logout_if_logged_in()
```

无需构造函数、with 报告块或模块级 test_execute。pytest 收集继承的 `test_process`；fixture 准备资源并调用 `TestCase.bind_resources(context, database)`，再调度 init/setup/teardown。context/database/aw/checks 的属性类型和赋值均显式定义在 TestCase 中，AW 与 BaseChecks 也由基类创建；在子类中可直接补全属性并跳转到类或方法定义。fixture 不再动态添加这些属性。初始化未完成也可能进入 teardown，恢复代码应处理属性尚不存在或业务只完成一部分的情况。一次普通方法返回不关闭当前标题。

PE 使用页面类内嵌元素类，PO 解析静态属性或实现相对定位；新增页面后在 locator.py 的登记函数中加入 PO。不要在 PE 中存储 Selenium WebElement，不要在用例中写 DOM 定位。

## 框架验证

```powershell
.\.venv\Scripts\python.exe -m pytest framework_checks -q
```

不启动浏览器，覆盖 PE 多条件与歧义检查、扩展字段校验、跳转登记、SQLite 幂等和凭据隔离、生命周期之外调用报告方法的错误、真实 pytest 子进程中的初始化/前置失败恢复与双重失败报告。子进程中故意失败的案例是框架验证输入，外层验证应全部通过。

运行记录见 `验证记录.md`。
