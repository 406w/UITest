# 实现验证记录

验证日期：2026-09-22。

## 后续增强：标题步骤开始截图

每次 step/checkpoint 进入 Allure 步骤后立即截图，附件名为“步骤开始：标题”。阶段调度将当前 Context 传入 StepReporter；无浏览器、关闭截图或截图失败时不阻断操作，沿用既有敏感区域隐藏机制。

报告相关检查 4 项通过，包括截图在动作前执行、附件归属、关闭截图、截图失败、无驱动及默认标题不重复截图。另使用真实 Edge 浏览器运行退出登录场景，1 passed，并生成 HTML。

验证运行 ID：`20260922-193642-80cba1`。检查原始结果和最终 HTML 数据，process 下的 4 个标题步骤均各有 1 张 PNG 附件，生成后的图片文件均存在；已目视检查退出登录之前的首页截图。

## 后续修复：pytest / IDE 默认报告

已在 pytest.ini 配置默认 Allure 报告目录、HTML 自动生成、日志级别及本机 Allure CLI。公共报告钩子在 allure-pytest 初始化前确定独立结果目录，测试结束生成 HTML 并写入 report/latest.json；main.py 复用同一配置。

新增 3 项报告回归检查全部通过：非工程工作目录直接运行时默认输出、通过/失败记录与标题步骤、不混合多次运行、显式结果目录覆盖、CLI 缺失时保留原始结果和测试失败状态，以及 collect-only 不生成报告。

完整框架检查为 9 passed、1 failed，唯一失败仍是当前固定账号读取逻辑与原有环境变量凭据测试不一致。HTML 成功生成，报告生成错误为空。此次检查记录 ID：`20260922-193048-890d67e7`，属于框架验证报告，不是重新运行 UI 业务场景的结果。

## 后续修复：直接 pytest / IDE 启动的临时目录权限

database fixture 已移除对 tmp_path_factory 的依赖，统一使用 `.runtime/ui-data/<会话UUID>/db.db`，不再访问系统的 `pytest-of-19469` 目录。

新增回归检查模拟系统临时目录抛出 PermissionError，从非工程工作目录直接启动两次 pytest，验证两次均可读取基础数据，且数据库路径和写入相互隔离。该检查通过。

本次框架检查结果为 6 passed、1 failed。失败项为原有凭据读取测试：当前 `data/database.py` 的 account 方法已被改为固定返回账号，未读取测试设置的密码环境变量；本次未修改该账号逻辑。下方“最终验证”为首次实现时的历史记录。

## 最终验证

- Python 3.12.14、pytest 8.4.2、Selenium 4.49.0、allure-pytest 2.16.1。
- Windows 无头 Microsoft Edge 153.0.4234.48，使用对应版本 EdgeDriver。
- Node.js 24.18.1 启动项目真实 ERP，独立端口和独立 SQLite 数据库。
- Allure CLI 2.45.0 成功生成 HTML 报告。
- 最终 UI 测试：**5 passed，21.59 秒**。
- 框架检查：**6 passed**。
- Python 源码编译检查通过。

最终运行 ID：`20260922-183228-d22e44`。

| 场景 | 结果 |
| --- | --- |
| 有效账号登录 ERP，刷新保留会话 | 通过 |
| 错误密码不能登录 | 通过 |
| 退出登录后刷新不能恢复会话 | 通过 |
| 添加销售订单（两行商品） | 通过 |
| 添加采购订单 | 通过 |

结果文件：[Allure 原始结果](report/allure-results/20260922-183228-d22e44/)、[Allure HTML 入口](report/allure-report/20260922-183228-d22e44/index.html)。普通报告需要通过 Allure 本地服务打开：

```powershell
& 'G:\Python\allure\allure-2.45.0\bin\allure.bat' open report/allure-report/20260922-183228-d22e44
```

检查原始结果确认各用例展示具体中文场景名称，标题步骤按调用顺序生成，所有最终用例状态为 passed。

## 恢复结果

通过只读 SQLite 查询复查最终隔离数据库：

- 本次销售单 1 张、采购单 1 张，均为 cancelled。
- sessions 表记录数为 0，测试会话已退出。
- 原始 4 张演示订单保留。
- 运行脚本已停止此次 Node 服务；数据库和服务日志保留在 `.runtime/20260922-183228-d22e44/`。
- 未修改原项目 8090 环境、前端或后端源码，未调用演示数据重置接口。

## 失败路径验证

另在隔离实例运行了一个预期失败的截图验证，不属于上述业务测试集。记录 ID 为 `20260922-182916-18f526`，存放于本地忽略的 `.runtime/verification/` 与对应报告目录。

确认：前一标题步骤为 passed，故意断言失败的 checkpoint 为 failed，原断言信息保留；截图及校验结果附在失败 checkpoint 内；teardown 仍执行。已目视检查截图，密码输入框及页面演示密码提示被隐藏。

框架自动检查还覆盖 init/setup 失败时跳过 process、仍执行 teardown，以及 process 与 teardown 同时失败时分别保留测试失败和清理错误。

## 环境说明

受限进程环境启动 Edge 时 GPU 子进程曾因权限错误退出；在正常本地进程权限下完成了全部浏览器验证，没有禁用浏览器安全沙箱。早期环境排查及预期失败的报告保留用于诊断，不是最终业务测试结果。
